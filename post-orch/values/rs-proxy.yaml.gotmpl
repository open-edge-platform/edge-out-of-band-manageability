# SPDX-FileCopyrightText: 2026 Intel Corporation
#
# SPDX-License-Identifier: Apache-2.0

withReleaseServiceToken: false

proxyTargetRegistry: {{ env "EOM_REGISTRY" | default "registry-rs.edgeorchestration.intel.com" }}
proxyTargetFiles: {{ env "EOM_FILE_SERVER" | default "files-rs.edgeorchestration.intel.com" }}

refreshInterval: "10m"

externalSecretsApiGroup: external-secrets.io/v1

{{- if env "EOM_HTTPS_PROXY" }}
env:
  {{- if env "EOM_HTTPS_PROXY" }}
  - name: https_proxy
    value: {{ env "EOM_HTTPS_PROXY" }}
  {{- end }}
  {{- if env "EOM_HTTP_PROXY" }}
  - name: http_proxy
    value: {{ env "EOM_HTTP_PROXY" }}
  {{- end }}
  {{- if env "EOM_NO_PROXY" }}
  - name: no_proxy
    value: {{ env "EOM_NO_PROXY" }}
  {{- end }}
{{- end }}
